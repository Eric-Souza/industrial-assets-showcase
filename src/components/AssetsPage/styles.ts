import styled from 'styled-components'

export const Container = styled.div`
  .card {
    display: flex;
    flex-direction: column;
    margin: 20px;

    max-width: 300px;
  }

  .card img {
    max-width: 450px;
    max-height: 220px;
  }
`
